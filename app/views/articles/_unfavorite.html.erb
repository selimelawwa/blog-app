<%= form_for(Favorite.find_by(user_id: current_user.id, article_id: article.id),
             html: { method: :delete },
             remote: true) do |f| %>
             <div><%= hidden_field_tag :article_id, article.id %></div>
            <%= f.submit "Unfavorite", class: "btn" %>
<% end %>

