<div id="follow_form">
	
	<% if !current_user.favors?(article) %>
		<%= render 'articles/favorite', article: article %>
	<% else %>
		<%= render 'articles/unfavorite', article: article %>
	<% end %>
	
</div>

